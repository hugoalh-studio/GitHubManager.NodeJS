const chalk = require("chalk");
const header = `${chalk.bgGray(`                                                  `)}
${chalk.bgGray.white.bold("    GitHub Manager (NodeJS CLI Edition) v1.1.0    ")}
${chalk.bgGray(`                                                  `)}`;
module.exports = header;
